# Ambiente
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# Segredos — nunca commitar
.env

# Dashboard web (Next.js)
web/node_modules/
web/.next/
web/out/
web/.vercel/
web/next-env.d.ts
web/*.tsbuildinfo

# Ilustrações dos posts VÃO para o git: o dashboard em produção lê data/media/
# via API do GitHub, e são JPEGs comprimidos (~150-250KB cada).
# Arquivos grandes/temporários de mídia ficam de fora:
data/media/**/*.png
data/media/**/*.tmp

# SO
.DS_Store
Thumbs.db
